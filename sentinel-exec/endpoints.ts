const BASE_URL = "http://localhost:8080/sentinel";

export const EXECUTABLES_URL = `${BASE_URL}/executables`;
export const SCANS_URL = `${BASE_URL}/scans`;
export const USERS_URL = `${BASE_URL}/users`;
export const AUTH_URL = `${BASE_URL}/auth`;
export const ADMIN_URL = `${BASE_URL}/admin`;